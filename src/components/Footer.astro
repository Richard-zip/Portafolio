---
const currentYear = new Date().getFullYear();

const links = [
  { label: 'GitHub', href: 'https://github.com/[TU_GITHUB]', isExternal: true },
  { label: 'LinkedIn', href: 'https://linkedin.com/in/[TU_LINKEDIN]', isExternal: true },
  { label: 'Email', href: 'mailto:[TU_EMAIL]', isExternal: false },
];
---

<footer class="w-full border-t border-neutral-200 dark:border-[#1f1f1f] bg-[#fafafa] dark:bg-[#0a0a0a] py-12 transition-colors duration-200 mt-auto">
  <div class="max-w-[1100px] mx-auto px-4 sm:px-6 lg:px-8 flex flex-col md:flex-row items-center justify-between gap-6">
    <!-- Copyright & Rol -->
    <div class="flex flex-col sm:flex-row items-center gap-2 sm:gap-4 text-xs text-neutral-500 dark:text-[#888888] font-mono">
      <span>© {currentYear} Richard Assis.</span>
      <span class="hidden sm:inline text-neutral-400 dark:text-neutral-700">•</span>
      <span>Backend Software Engineer</span>
    </div>

    <!-- Enlaces externos -->
    <div class="flex items-center gap-6 text-xs font-mono">
      {links.map((link) => (
        <a
          href={link.href}
          target={link.isExternal ? '_blank' : undefined}
          rel={link.isExternal ? 'noopener noreferrer' : undefined}
          class="text-neutral-600 dark:text-[#888888] hover:text-emerald-600 dark:hover:text-emerald-400 transition-colors"
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>
</footer>
